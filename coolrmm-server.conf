# CoolRMM Server Configuration
# Copy this file to coolrmm-server.conf and edit before running the server.
# coolrmm-server.conf must sit in the same directory as coolrmm-agent.exe.

# server_url — the full address of your CoolRMM server.
# Replace with your server's actual IP address and port.
# api_key - created randomly
api_key=TroubleDownTheLine.68